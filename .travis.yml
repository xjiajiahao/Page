language: node_js
node_js:
- "4"
branches:
  only:
  - master
before_install:
- openssl aes-256-cbc -K $encrypted_c8313b134a2a_key -iv $encrypted_c8313b134a2a_iv -in .travis/travis.enc -out ~/.ssh/travis -d
- chmod 600 ~/.ssh/travis
- eval $(ssh-agent)
- ssh-add ~/.ssh/travis
- cp .travis/ssh_config ~/.ssh/config
- git config --global user.name 'Stephen Tse'
- git config --global user.email xjiajiahao@gmail.com
#- npm install -g hexo-cli
install:
- npm install hexo-cli -g
- npm install
script:
- npm run deploy
#- git submodule init
#- git submodule update
#- hexo generate
